---
type: 实验记录
ex_id: EX-20260607T131209Z-main-A22N
rd_id: RD-20260605T133318Z-main-H6V3
status: completed
stage: formal_strict_pass_a22n_native_equivalence
owner: main
created_at: 2026-06-07T13:12:09Z
updated_at: 2026-06-07T23:18:00+08:00
platform_project: ${QUANT_PLATFORM_ROOT}
config_paths:
  - configs/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/
result_paths:
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/logs/formal/
summary_paths:
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/summary/formal/
quality_gate: L2_engineering_equivalence_strict_pass
decision_ids: [DEC-20260607T125847Z-main-WQ8R, DEC-20260607T151252Z-main-A22N]
subagent_call_ids: [SUB-20260607T132400Z-main-A22N-SCRIPTCHK, SUB-20260607T134000Z-main-A22NDOC]
subagent_exemption:
tags: [双池轮动, score过热, A22, 原生字段, 等价审计, formal]
---

# A22原生字段等价审计预注册

## 关联链接

- 方向：[[02_研究方向/RD-20260605T133318Z-main-H6V3_双池轮动score过热拥挤机制模块|双池轮动 score 过热拥挤机制模块]]
- 上游决策：[[05_研究决策/DEC-20260607T125847Z-main-WQ8R_K3AC同成本通过后A22优先于A23决策|WQ8R K3AC 后 A22 优先]]
- 结果决策：[[05_研究决策/DEC-20260607T151252Z-main-A22N_A22原生字段等价通过后工程化决策|A22N 原生字段等价通过后工程化决策]]
- 上游实验：[[04_实验记录/EX-20260607T103055Z-main-K3AC_A23同成本配对formal AB预注册|K3AC 同成本配对]]
- 研究质量审计：[[08_方法论/研究质量审计规范|研究质量审计规范]]
- 子代理规范：[[08_方法论/子代理调度规范|子代理调度规范]]

## 新手解释

前面我们找到的最好候选是 A22 cap70：高分不再完全过滤，而是把风险仓位压到 70%。但旧 A22 配置其实借用了早期 A13 的字段名，所以日志会写成 `A13_SCORE56_BUDGET`。这会让后续上线、审计和排错变得不清楚。

本实验不寻找更高收益，也不调新参数。它只问一个工程问题：把同一个 A22 cap70 逻辑从旧 A13 字段迁移到原生 A22 字段后，回测结果是否完全一样。

如果完全一样，说明我们可以清理字段和日志语义；如果不一样，说明原生实现和旧实现存在行为差异，不能继续推进默认切换。

## 研究问题

在同一个实验、同一代码状态、同一数据环境中，`legacy_a13_cap70` 与 `native_a22_cap70` 是否能产生完全一致的交易路径和组合结果？

## 假设

### 正向假设

`native_a22_cap70` 只是把旧 A22 cap70 的字段表达从 `r010a13_score56_*` 映射为 `r010a22_hot_score_budget_*`，不改变信号、交易、仓位或成本。因此它应与 `legacy_a13_cap70` 在四个历史分段上完全等价。

### 反向假设

如果原生 A22 字段在调用顺序、触发条件、目标 ETF 检查、风险仓计算或日志副作用上与旧 A13 字段不同，则两组结果会出现 final、MDD、交易数、费用或关键文件 hash 差异。

## 样本划分

| 分段 | 用途 |
| --- | --- |
| `2020_2021` | 压力与高分过热早期样本 |
| `2022_2023` | 回撤和震荡样本 |
| `2024` | 负控段，历史上 A22 与 hard5 多次相同 |
| `2025_20260519` | 近端强趋势样本 |

## 实验设计

| variant | 含义 | 配置来源 |
| --- | --- | --- |
| `legacy_a13_cap70` | 旧字段表达，保留 `r010a13_score56_risk_budget_enabled=true`、`risk_cap=0.70` | `configs/research/R010-A22/hot_score_budget/a20_score_ge5_cap70/` |
| `native_a22_cap70` | 原生字段表达，关闭 A13，开启 `r010a22_hot_score_budget_enabled=true`、`base_risk_cap=0.70`、`state_tier_enabled=false` | 由同一旧配置转换生成 |

固定条件：

- 不改变 `max_score=9999` 和 `score_hot_filter_mode=conditional_hot_state`。
- 不改变交易成本、回测区间、数据需求、B3/A2 等其他策略字段。
- 不启用 A23 的 broad-blowoff 分层。
- 同一实验中同时跑旧字段版和原生字段版，避免跨时间环境漂移。

## 通过门槛

必须全部满足：

- 8/8 回测完成并有 `summary.json`。
- 8/8 有 run log、manifest，且 `exit_status=0`。
- 8/8 配置字段审计通过。
- 原生 vs 旧字段 4/4 分段 `final_value` 完全一致。
- 原生 vs 旧字段 4/4 分段 `max_drawdown` 完全一致。
- 原生 vs 旧字段 4/4 分段交易数和费用完全一致。
- `equity_curve.csv`、`positions.csv`、`subportfolio_equity.csv` hash 4/4 完全一致。
- 忽略 `order_id` 后的 `trades.csv` 归一化 hash 4/4 完全一致。

## 失败条件

出现任一情况即失败：

- 任一分段缺少结果、日志、manifest 或 exit status 非 0。
- 配置字段映射不满足旧 A13 与原生 A22 的一一对应。
- 任一分段 final、MDD、交易数、费用或关键 hash 不一致。
- 日志出现 `Traceback`、`ERROR`、`SystemError` 等风险标记。

失败后不允许把原生 A22 作为 hard5 替代候选继续推进；必须先定位代码差异。

## 未来函数审计

| 检查项 | 预注册判断 | 证据 |
| --- | --- | --- |
| 数据时间戳 | 沿用上游 A22 配置，不新增数据字段 | 本实验只转换配置字段，不接入新数据 |
| 信号滞后 | 不改变信号计算入口 | 使用同一策略文件和同一回测引擎 |
| 交易价格 | 不改变成交模型 | 交易成本和执行字段保持原样 |
| Shadow 标签 | 不启用 shadow 或观察信号交易化 | 配置只打开 A22 原生预算，不改默认策略 |
| 未来函数最终确认 | 待结果后主控复核 | 若等价通过，也只证明原生字段等价，不证明 A22 可上线 |

## 过拟合审计

| 检查项 | 预注册判断 |
| --- | --- |
| 实验预注册 | 本卡在 formal 运行前建立 |
| 参数范围 | 无参数搜索，仅固定 cap70 等价审计 |
| 多重比较 | 仅两组配对，不选最优 |
| 成本扰动 | 不做新增成本扰动；上游 K3AC 已做同成本扰动，本实验仅验证字段等价 |
| 消融或负控 | `legacy_a13_cap70` 是同实验控制组；2024 继续作为行为负控段 |
| 样本外 | 本实验不升级生产，只服务工程清理；forward/shadow 需另开 |

证据等级预期：若 strict 通过，为 `L2_engineering_equivalence`，只能支持“原生 A22 字段可替代旧字段表达”，不能直接支持生产默认切换。

## 子代理调用记录

适配判断：`适合调用`

调用状态：`called_completed`

| 调用 ID | 平台昵称 | 任务代号 | 模型 | 发起时间 | 读取文件 | 修改文件 | 执行命令 | 结论边界 | 风险点 | 主控复核 | 结果对决策影响 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SUB-20260607T132400Z-main-A22N-SCRIPTCHK | Lorentz | SUBTASK-20260607T132400Z-main-A22NSC_子查_A22N脚本字段与strict规则审计 | gpt-5.3-codex-spark | 2026-06-07T13:24:00Z | A22N 生成脚本、运行脚本、汇总脚本、formal manifest 与 8 个配置 | 无 | `Get-ChildItem`、`Test-Path`、`Get-Content`、`rg`、`ConvertFrom-Json` 只读检查 | 字段映射与脚本约束链条自洽；未执行回测，不能确认最终数值等价 | 配置 hash 未纳入 strict、日志风险关键词有限、多 stamp/环境变量覆盖需主控复核 | 主控采纳脚本链条可跑的结论，并保留上述审计边界；strict 后确认本轮最新 stamp 为 `20260607T132044Z` | 支持 formal strict 执行；不改变 A22 是否 promote 的最终判断 |
| SUB-20260607T134000Z-main-A22NDOC | Dirac | SUBTASK-20260607T134000Z-main-A22NDOC_子查_A22N完工文档字段清单 | gpt-5.3-codex-spark | 2026-06-07T13:40:00Z | A22N 实验卡、实验台账、子代理台账、入口页、方向页 | 无 | 只读字段核对 | strict 未完成前只给收口字段清单，不判断等价 | 发现实验台账中 A22N 有重复行；提示 strict 通过/失败两套字段更新边界 | 主控采纳台账去重和入口同步清单；最终等价判断由 strict summary 和主控复核给出 | 支持文档收口完整性，不作为收益或 promote 证据 |

台账行：已同步 `01_台账/子代理调用台账.csv`。

## 执行计划

```bash
PYTHONIOENCODING=utf-8 python3 -m py_compile scripts/research/generate_a22n_native_hot_budget_configs.py scripts/research/summarize_a22n_native_hot_budget_equivalence.py
bash -n scripts/research/run_a22n_native_hot_budget_equivalence.sh
PYTHONIOENCODING=utf-8 python3 scripts/research/generate_a22n_native_hot_budget_configs.py
DRY_RUN=1 bash scripts/research/run_a22n_native_hot_budget_equivalence.sh
PYTHONUNBUFFERED=1 PYTHONIOENCODING=utf-8 bash scripts/research/run_a22n_native_hot_budget_equivalence.sh 2>&1 | tee results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/logs/formal/runner.log
PYTHONIOENCODING=utf-8 python3 scripts/research/summarize_a22n_native_hot_budget_equivalence.py --strict
```

## 执行记录

### 平台脚本

```text
${QUANT_PLATFORM_ROOT}/scripts/research/generate_a22n_native_hot_budget_configs.py
${QUANT_PLATFORM_ROOT}/scripts/research/run_a22n_native_hot_budget_equivalence.sh
${QUANT_PLATFORM_ROOT}/scripts/research/summarize_a22n_native_hot_budget_equivalence.py
```

### 运行命令

```bash
cd /mnt/e/qp_v1_4_0
PYTHONIOENCODING=utf-8 python3 -m py_compile scripts/research/generate_a22n_native_hot_budget_configs.py scripts/research/summarize_a22n_native_hot_budget_equivalence.py
bash -n scripts/research/run_a22n_native_hot_budget_equivalence.sh
PYTHONIOENCODING=utf-8 python3 scripts/research/generate_a22n_native_hot_budget_configs.py
DRY_RUN=1 bash scripts/research/run_a22n_native_hot_budget_equivalence.sh
PYTHONUNBUFFERED=1 PYTHONIOENCODING=utf-8 bash scripts/research/run_a22n_native_hot_budget_equivalence.sh 2>&1 | tee results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/logs/formal/runner.log
PYTHONIOENCODING=utf-8 python3 scripts/research/summarize_a22n_native_hot_budget_equivalence.py --strict
```

### 可见进度与日志

- 运行范围：2 个 variant × 4 个分段，共 8 段 formal。
- 运行方式：串行前台运行，`PYTHONUNBUFFERED=1`，每段 `run.log` 与总 `runner.log` 同步输出。
- 日志根：`${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/logs/formal/20260607T132044Z/`
- 总日志：`${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/logs/formal/runner.log`
- strict summary：`${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/summary/formal/summary.json`

### 执行偏差

- A22N 启动时运行脚本未检测到竞争回测；运行过程中观察到平台上有另一个非 A22N 回测进程并行运行。该偏差可能影响耗时，但 strict 结果显示 8/8 `exit_status=0`、日志无风险标记、文件级 hash 完全一致，因此不否决本轮工程等价证据。
- strict 脚本未把 config sha 纳入最终 gate；但每段 manifest 已记录 `config_sha256`，汇总脚本执行了 `config_fields_ok` 静态检查。该风险保留为后续工程审计改进项，不影响本次“旧字段表达与原生字段表达等价”的结论。

## 实际观察

Strict 汇总结果：

- `expected_runs=8`
- `completed_runs=8`
- `strict_missing=[]`
- `all_gates_pass=true`
- `all_evidence_complete=true`
- `final_value_exact_4of4=true`
- `max_drawdown_exact_4of4=true`
- `trade_count_exact_4of4=true`
- `fee_sum_exact_4of4=true`
- `equity_curve_hash_exact_4of4=true`
- `positions_hash_exact_4of4=true`
- `subportfolio_equity_hash_exact_4of4=true`
- `trades_normalized_hash_exact_4of4=true`

| 分段 | legacy final | native final | MDD | trades | fee | diff |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `2020_2021` | `225931.41` | `225931.41` | `-20.8378%` | `585` | `7530.69` | `0` |
| `2022_2023` | `138583.63` | `138583.63` | `-24.3881%` | `463` | `4718.07` | `0` |
| `2024` | `167005.22` | `167005.22` | `-26.9936%` | `281` | `3090.18` | `0` |
| `2025_20260519` | `350312.39` | `350312.39` | `-14.9040%` | `506` | `7451.11` | `0` |

逐段 `equity_curve.csv`、`positions.csv`、`subportfolio_equity.csv`、忽略 `order_id` 后的 `trades.csv` 归一化 hash 全部一致。

## 和预测一致或不一致

- 一致：原生 A22 flat cap70 与 legacy A13 字段表达在四段结果、交易路径、费用和关键文件 hash 上完全等价。
- 一致：2025_20260519 强趋势段没有被原生字段迁移错过，最终权益和交易数均完全一致。
- 一致：2024 负控段也完全一致，说明无触发或低触发环境下迁移没有引入副作用。
- 不一致：无实质结果偏差。唯一流程偏差是运行期间出现另一个非 A22N 回测进程，已记录为耗时风险而不是结果差异来源。

## 支持证据

- `summary/formal/summary.json`：`strict_missing=[]`、`completed_runs=8`、`all_gates_pass=true`。
- `comparison_pairs.csv`：四段 `final_value_diff/max_drawdown_diff/trade_diff/fee_sum_diff` 全为 `0.0`，四类 hash equality 全为 `True`。
- `segment_metrics.csv`：8 段 `config_fields_ok=True`、`exit_status=0`、`log_has_risk=False`。
- 各段 run log 和 manifest 齐全，运行脚本最终输出 `A22N native equivalence completed`。

## 反对证据

- 本实验只证明字段迁移工程等价，不证明 A22 cap70 可直接替代 hard5 进入生产默认。
- strict gate 没有直接比较 config sha；后续可把 manifest 中的 `config_sha256` 纳入汇总 gate。
- 本实验没有新增 forward、shadow、样本外或实盘执行审计；未来函数最终确认仍需在下一阶段完成。

## 对新手的短总结

A22 原来“看起来叫 A22”，实际配置还是借用 A13 的字段名。A22N 做的是清理这层命名债务：把旧字段换成真正的 A22 字段，再把四段历史完全重跑一遍。

结果是：旧字段版和新字段版每一段收益、回撤、交易数、费用，以及关键结果文件 hash 都完全一样。这说明可以把 A22 cap70 的实现改成原生 A22 字段，不会改变历史交易路径。

但这还不是上线结论。它只解决“字段是否等价”的工程问题；A22 是否能替代 hard5，还要继续做未来函数审计、forward/shadow 边界和默认关闭观察。

## 研究判断

建议状态：`completed / formal_strict_pass_a22n_native_equivalence`

结论：`native_a22_cap70` 通过工程等价门槛，可以作为旧 A13 字段表达的原生替代。A22 cap70 的历史收益证据现在可以写成“旧字段表达与原生字段表达已验证等价”，但不能写成“已生产 promote”。

是否需要研究决策卡：新增 `DEC-20260607T151252Z-main-A22N`。A22N 没有改变 WQ8R 的收益路线选择，也没有授权 A22 替代 hard5 上线；它只把 QXSS 暴露的字段语义阻塞解除，允许 A22 cap70 从旧 A13 字段表达迁移为原生 A22 字段表达。

## 下一步

1. 将 A22 后续配置和日志清理为 `r010a22_hot_score_budget_*` 原生字段表达，保持 `state_tier_enabled=false`，避免与 A23 broad-blowoff 分层混淆。
2. 新开 A22 native forward/shadow 边界验证：默认仍保留 hard5，A22 只允许作为默认关闭观察候选。
3. 补一轮未来函数二次审计：确认 `signal_date/trade_date`、目标 ETF momentum 回退路径、日志触发和实盘配置映射。
