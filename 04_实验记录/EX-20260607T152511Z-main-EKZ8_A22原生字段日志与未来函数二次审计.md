---
type: 实验记录
ex_id: EX-20260607T152511Z-main-EKZ8
rd_id: RD-20260605T133318Z-main-H6V3
status: completed
stage: readonly_future_log_audit_pass
owner: main
created_at: 2026-06-07T15:25:11Z
updated_at: 2026-06-07T15:34:19Z
strategy_id: STRAT-20260605T115651Z-main-DP00
module_type: 核心轮动风控诊断模块
decision_ids: [DEC-20260607T151252Z-main-A22N]
lit_ids: []
idea_ids: []
platform_project: ${QUANT_PLATFORM_ROOT}
config_paths:
  - configs/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/
  - scripts/research/audit_ekz8_a22_native_logs_future.py
result_paths:
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/
summary_paths:
  - results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/summary.json
quality_gate: L1_readonly_future_log_audit_pass
subagent_call_ids: [SUB-20260607T152000Z-main-A22FWD-ENTRY]
subagent_exemption:
tags: [双池轮动, score过热, A22, 原生字段, 未来函数审计, 日志审计, 只读审计]
---

# A22原生字段日志与未来函数二次审计

## 关联链接

- 研究方向：[[02_研究方向/RD-20260605T133318Z-main-H6V3_双池轮动score过热拥挤机制模块|双池轮动 score 过热拥挤机制模块]]
- 策略档案：[[03_策略档案/STRAT-20260605T115651Z-main-DP00_双池轮动策略档案|双池轮动策略档案]]
- 上游实验：[[04_实验记录/EX-20260607T131209Z-main-A22N_A22原生字段等价审计预注册|A22N 原生字段等价审计]]
- 上游决策：[[05_研究决策/DEC-20260607T151252Z-main-A22N_A22原生字段等价通过后工程化决策|A22N 工程化决策]]
- 研究质量审计：[[08_方法论/研究质量审计规范|研究质量审计规范]]
- 子代理调用台账：[[01_台账/子代理调用台账.csv|子代理调用台账]]

## 1. 新手摘要

这次实验想知道：A22N 证明结果文件完全一致后，日志语义和未来函数边界是否也干净。

我们原本预计：native A22 版本应只出现 `A22_HOT_BUDGET` 指纹，不再出现 `A13_SCORE56_BUDGET`；每个动作日志里的 `signal_date` 应不晚于 `trade_date`；legacy 与 native 的预算触发次数应该按分段一一相同。

实际看到：只读 strict 通过。8/8 配置字段、8/8 run log、8/8 可解析日期顺序、8/8 manifest config sha 匹配均通过；native 触发分段只出现 A22 指纹，legacy 触发分段只出现 A13 指纹，2024 是双方 0 触发负控。

这说明：A22N 的 formal 产物在日志语义和可见 `signal_date <= trade_date` 边界上没有发现阻塞项，可以进入下一步 A22 native forward/shadow 边界验证预注册。

但还不能说明：即使通过，也只说明 A22N 这组历史回测日志和日期边界没有发现问题；它不等于实盘或 shadow 已可上线。

下一步要做：新开 A22 native forward/shadow 边界验证预注册，显式互斥 A13/A22，并补样本边界、日志指纹、日期顺序、manifest 和 hash gate。

## 2. 研究背景

A22N 已经证明 legacy A13 字段表达与 native A22 flat cap70 在四段结果文件上完全等价。剩余问题是：字段迁移后，日志是否真的从 A13 语义切到 A22 语义；A22 预算触发动作是否存在 `signal_date > trade_date` 的未来函数迹象；以及 manifest/config sha 是否能支持后续工程清理。

本实验只读 A22N 的既有产物，不运行新回测、不更新数据、不改变策略逻辑。

## 3. 实验前假设

`native_a22_cap70` 在 A22N 结果中应表现为日志语义上的原生 A22：配置关闭 A13、开启 A22；触发事件使用 `A22_HOT_BUDGET`；日期顺序不出现未来函数；触发次数与 legacy A13 字段表达按分段完全相同。

## 4. 实验前预测

如果假设为真，应该看到：

- 指标：8/8 配置字段通过；8/8 manifest 存在 `config_sha256`；8/8 run log `exit_status=0` 且无风险关键词。
- 交易行为：native 四段 `A22_HOT_BUDGET` 触发次数分别应等于 legacy A13 的对应触发次数；native 不应出现 `A13_SCORE56_BUDGET`。
- 风险表现：本实验不新增收益判断，只复核 A22N strict 已通过的结果路径。
- 分段表现：2020_2021、2022_2023、2025_20260519 应看到预算触发，2024 可能仍为 0 触发负控。

## 5. 基准和对照

| 对照 | 用途 | 路径 |
| --- | --- | --- |
| `legacy_a13_cap70` | 旧字段日志指纹和触发次数对照 | `${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/legacy_a13_cap70/` |
| `native_a22_cap70` | 原生字段日志语义与日期顺序审计对象 | `${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/native_a22_cap70/` |
| A22N strict summary | 确认结果文件等价已经成立 | `${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/summary/formal/summary.json` |

## 6. 竞争性解释

即使结果符合预期，也可能是：

- `logs.jsonl` 中不同模块的日志格式不完全一致，字符串指纹只能作为语义证据，不能替代代码审计。
- 2024 无触发只能证明无触发负控，不提供触发路径证据。
- `signal_date <= trade_date` 只能排除日志中可见的未来日期，不能证明所有数据源都已完成实盘级可得日审计。
- manifest 记录 config sha，但若不和台账锁定版本，仍可能出现后续配置漂移。

## 7. 证伪条件

出现以下情况，本假设不通过：

- native 配置仍启用 `r010a13_score56_risk_budget_enabled=true`。
- native `logs.jsonl` 出现 `A13_SCORE56_BUDGET` 或 `A13 score5-6风险预算触发`。
- native 四段 `A22_HOT_BUDGET` 触发次数与 legacy A13 对应分段不一致。
- 任一 R010-A/R010-B/R010-B1 日志存在 `signal_date > trade_date`。
- 任一 run log 缺失、`exit_status` 非 0 或出现 `Traceback/ERROR/SystemError`。

## 8. 未来函数审计

| 检查项 | 预注册判断 | 证据 |
| --- | --- | --- |
| 数据时间戳只使用当时可得信息 | 待检查 | 审计 `signal_date/trade_date` |
| 信号生成和成交价格不存在同 bar 泄漏 | 待检查 | 审计动作日志日期顺序；不新增成交模型 |
| 股票池或 ETF 池不存在未来成分泄漏 | 继承上游 | 本实验不改 ETF 池 |
| 财务、宏观或估值数据按可得日处理 | 不适用 | A22 不引入财务、宏观或估值字段 |
| Shadow 或观察信号未被当成默认交易信号 | 待检查 | 本实验只读 formal；不启用 live/shadow 默认 |

负控或错位检查：

- 2024 分段作为无触发负控。
- legacy 与 native 触发次数错位视为失败，不做事后解释。

## 9. 过拟合审计

| 检查项 | 预注册判断 |
| --- | --- |
| 参数搜索空间已预注册 | 通过；不新增参数 |
| 样本内、验证集、样本外划分清楚 | 继承 A22N 四段固定样本 |
| 邻近参数敏感性合理 | 不适用；只读字段审计 |
| 成本、滑点或换手扰动已检查 | 继承 K3AC/A22N；本实验不看收益优劣 |
| 已做消融或负控 | 部分；2024 无触发负控 |
| 未只报告最优结果 | 通过；8 段日志全部纳入 |

证据等级预期：若通过，为 `L1_readonly_future_log_audit`；只能支持“日志语义和可见日期边界通过只读审计”。

## 10. 子代理调用记录

适配判断：`适合调用`

调用状态：`completed`

| 调用 ID | 平台昵称 | 任务代号 | 模型 | 发起时间 | 读取文件 | 修改文件 | 执行命令 | 结论边界 | 风险点 | 主控复核 | 结果对决策影响 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SUB-20260607T152000Z-main-A22FWD-ENTRY | Boole | SUBTASK-20260607T152000Z-main-A22FWD_子查_A22原生forward与未来函数审计入口 | gpt-5.3-codex-spark | 2026-06-07T15:20:00Z | 研究库 A22N/决策/方向页，平台源码、配置和脚本入口 | 无 | 只读盘点 A22 native 字段审计、A22N 配置/运行/汇总脚本、shadow/forward 可复用入口和风险清单 | 建议沿用 A22N 配对结构但不要把等价脚本直接当 forward/shadow 全流程；提示 A13/A22 双开会有顺序干扰；日志审计不能替代最终未来函数裁决 | 目标 ETF 动量回退、双开覆盖顺序、缺少 A22 native forward/shadow 专用工程脚本 | 主控采纳入口清单和风险提示；最终 strict 审计由 `audit_ekz8_a22_native_logs_future.py` 完成 | 支持下一步新开 A22 native forward/shadow 边界验证，但不支持直接 promote 或实盘 |

台账行：已同步 `01_台账/子代理调用台账.csv`。

## 11. 执行记录

### 平台配置

```text
${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T131209Z-main-A22N/formal/
${QUANT_PLATFORM_ROOT}/scripts/research/audit_ekz8_a22_native_logs_future.py
```

### 计划运行命令

```bash
cd /mnt/e/qp_v1_4_0
PYTHONIOENCODING=utf-8 python3 -m py_compile scripts/research/audit_ekz8_a22_native_logs_future.py
PYTHONIOENCODING=utf-8 python3 scripts/research/audit_ekz8_a22_native_logs_future.py --strict
```

### 可见进度与日志

- 是否过程可见：是。只读审计脚本会输出 JSON summary，不运行回测。
- 日志路径：`${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/audit_stdout.json`
- 查看进度命令：不适用，预计为短时只读脚本。
- 异常判断：脚本返回非 0 或 summary 中任一 strict gate 失败则本实验失败。
- 后台回测豁免：未计划后台运行；未计划回测。

### 结果路径

```text
${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/summary.json
${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/log_event_audit.csv
${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/config_manifest_audit.csv
```

## 12. 实际观察

执行命令：

```bash
cd /mnt/e/qp_v1_4_0
PYTHONIOENCODING=utf-8 python3 -m py_compile scripts/research/audit_ekz8_a22_native_logs_future.py
set -o pipefail && PYTHONIOENCODING=utf-8 python3 scripts/research/audit_ekz8_a22_native_logs_future.py --strict | tee results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/audit_stdout.json
```

实际结果：

- `completed_rows=8/8`。
- `all_gates_pass=true`。
- `config_fields_ok=8/8`，native 关闭 A13 且开启 A22，legacy 启用 A13 且未启用 A22。
- `run_logs_ok=8/8`，run log 均存在、`exit_status=0`，未发现 `Traceback/ERROR/SystemError/Exception ignored`。
- `manifest_config_sha_match=8/8`。
- `signal_date_order_ok=8/8`，可解析日期记录总计：legacy/native 各分段分别为 `972/968/484/660`，全部 `signal_date_violation_count=0`。
- 触发次数按分段完全一致：2020_2021 `9 vs 9`，2022_2023 `6 vs 6`，2024 `0 vs 0`，2025_20260519 `34 vs 34`。
- native 四段错误 A13 指纹数为 0；legacy 四段错误 A22 指纹数为 0；2024 为双方 0 触发负控。

## 13. 支持证据

- `summary.json`：`${QUANT_PLATFORM_ROOT}/results/v2/research/R010-A22/native_hot_score_budget/EX-20260607T152511Z-main-EKZ8/future_audit/summary.json`。
- `log_event_audit.csv` 记录 8 个 variant/segment 的配置、manifest、run log、日志指纹和日期顺序。
- `config_manifest_audit.csv` 记录 4 个分段的 legacy/native 触发次数配对与 2024 零触发负控。
- A22N 上游 strict 已证明同一批 legacy/native 结果在 final/MDD/交易数/费用和关键 hash 上完全一致，本实验补上日志语义与可见日期顺序。

## 14. 反对证据

- 本实验只读 A22N formal 产物，没有覆盖 forward/shadow 专用边界。
- 日志中 `signal_date <= trade_date` 只能排除可见日志字段的日期反转，不能单独证明全链路无未来函数。
- 2024 作为零触发负控只能证明“无触发时不应出现错误指纹”，不能提供 A22 触发路径正证据。
- Boole 提醒当前未见完整 A22 native forward/shadow 专用工程脚本，不能把 A22N 等价批处理直接当作 forward/shadow 全流程。

## 15. 偏差诊断

本轮第一次 strict 曾因为把 2024 零触发负控误判为“缺少 A22 正确指纹”而失败。主控按预注册预测修正 gate：2024 只有在 legacy/native 均为 0 触发且错误指纹为 0 时才通过；触发分段仍要求正确指纹存在、错误指纹为 0、触发次数一致。

另一个偏差边界是日志字符串审计只能确认“产物中写出的语义”，不能替代策略源码、数据可得日、成交模型和 forward/shadow 样本边界审计。

## 16. 研究判断

建议状态：`completed / L1_readonly_future_log_audit_pass`。

EKZ8 通过，说明 A22N 的 native A22 formal 产物在配置字段、日志指纹、manifest/config sha、run log 和可见 `signal_date <= trade_date` 上没有发现阻塞项。它支持继续进入 A22 native forward/shadow 边界验证预注册。

它不支持直接 promote A22，不改变默认 hard5，也不构成“无未来函数”最终裁决。

## 17. 下一步

新开 A22 native forward/shadow 边界验证。预注册时必须显式互斥 A13/A22，不允许双开作为主实验；沿用 A22N 的配对结构，但要补 forward/shadow 样本边界、manifest、日志指纹、`signal_date/trade_date` 和产物 hash gate。若缺少专用脚本，先做最小工程脚本而不是复用等价脚本硬跑。
