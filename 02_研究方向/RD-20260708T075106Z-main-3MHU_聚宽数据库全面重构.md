---
type: 研究方向
rd_id: RD-20260708T075106Z-main-3MHU
parent_rd_id: 
scope: 策略
module_type:
status: active
priority: P1
owner: main
created_at: 2026-07-08T07:51:06Z
updated_at: 2026-07-18T08:45:00Z
strategy_id:
current_decision_id:
current_best_ex_id: EX-20260708T075117Z-main-6WED
tags:
  - 聚宽
  - 数据库重构
  - 基础设施
  - 口径切换
---

# 聚宽数据库全面重构

## 关联链接

- 父方向：无（本方向为平台级基础设施工程，不属于任何单一策略）
- 子方向：无（按阶段推进，不拆模块子方向）
- 策略档案：无（影响全部策略的数据口径，不绑定单一策略档案）
- 当前最佳实验：[[04_实验记录/EX-20260708T075117Z-main-6WED_阶段0基础设施验证|阶段0基础设施验证（EX-JQDB-S0）]]
- 当前决策：无（阶段0为基础设施验证，无路线分叉；阶段1的路由切换+回归测试通过后可能需要 DEC）
- 历史规格文档：`docs/superpowers/specs/2026-07-08-jq-database-overhaul-design.md`（研究库内历史设计证据）
- 当前平台文档：`${QUANT_PLATFORM_ROOT}/docs/jq-v3-data-import-contract.md`、`${QUANT_PLATFORM_ROOT}/docs/jq-v3-project-manual.md`
- 研究驾驶舱：[[00_入口/研究驾驶舱|研究驾驶舱]]
- 历史实施分支：`feat/jq-database-overhaul`（commit 14f8baa..0593596，仅作迁移证据）

## 一句话说明

本方向最初负责把旧 `jq_*` 的 xtdata 混合口径切换为聚宽原生口径。当前 V2 已使用纯 `jq_v3` 新表和 `data_routing`，方向继续承担数据完整性、导入契约和扩展数据覆盖的研究记录，不再把旧 `quant_v2` 目录或 V1.4 表当作当前实现。

## 研究对象

- 所属策略：无（平台级基础设施，影响全部策略）
- 所属模块：数据层基础设施
- 历史生产入口：`E:\xtquant\策略\xtdata_update_full.py`（只说明重构前来源，不再执行）
- 当前 fetcher：`${QUANT_PLATFORM_ROOT}/src/quant_n3/jq_fetcher/`
- 当前每日更新器：`${QUANT_PLATFORM_ROOT}/src/quant_n3/jq_updater/`、`${QUANT_PLATFORM_ROOT}/scripts/jq_v3/`
- 当前数据路由：`${QUANT_PLATFORM_ROOT}/configs/data_routing.json`
- 当前导入契约：`${QUANT_PLATFORM_ROOT}/docs/jq-v3-data-import-contract.md`
- 历史阶段 0 benchmark：`${LEGACY_QUANT_PLATFORM_ROOT}/tmp/jq_speed_benchmark_report.txt`（只读证据）

## 历史背景问题（重构前已核实）

重构前的旧平台 13 张主表存在系统性口径错配：

- 表名前缀是 `jq_`、配置里 `provider` 标的是 `jq`，但实际数据**全部**来自 xtdata 口径。
- 生产入口 `E:\xtquant\策略\xtdata_update_full.py` 走 xtquant 拉数，不是 jqdatasdk。
- 已确认的下游症状：
  - `jq_valuation` 的 PE/PB/PS 字段全空。
  - `jq_stock_info` 的行业分类全是 `None`。
  - 复权因子与聚宽不等价。

这是 [[02_研究方向/RD-20260707T135354Z-main-WUFU_五福闹新春ETF动量轮动复现与审计|WUFU 策略复现差异]] 的根因之一——复现差异中 4.66x 的倍数关系，部分来自复权因子口径偏差。本方向就是修复这个基础设施的工程。

## 三项架构决策（已确定）

1. **接入方式 = jqdatasdk 纯 API 直连**。不用离线导出兜底，所有数据（含分钟线）都走 jqdatasdk 实时拉取。好处是口径与聚宽官方完全一致、可自动续传、不存在二次同步漂移。
2. **隔离策略 = 新建 `jq_v3_*` 表 + `data_routing` 路由切换**。旧 `jq_*` 表保留不动，新数据写入 `jq_v3_*`，通过路由层切换读取源，可随时回滚到旧表。
3. **范围 = 全面接入**。不仅切回 13 张主表的口径，还要接入聚宽全部特有数据：北向资金、分红送转、可转债、融资融券、因子库等。分钟线也纯 API 拉取，不退化到离线补量。

## 当前状态与研究信念

纯 jq_v3 新表和路由切换已经在 V2 落地，五福也已在该口径上完成原生复现。当前问题从“是否采用 jq_v3”转为“如何持续保证 2020-2026 数据完整、导入可恢复、配额可审计、前复权与未复权边界清楚”。历史阶段 0 benchmark 只作为早期可行性证据，不再代表当前下载量或剩余配额。

## 可观察预测

如果重构成功，应该能看到：

- `jq_valuation`（或其 v3 继任者）PE/PB/PS 非空，数值与聚宽官方一致。
- `jq_stock_info`（或 v3）行业分类非 None，与聚宽官方一致。
- 用聚宽官方策略代码回测，权益曲线与聚宽官方记录对齐。
- WUFU 等策略复现时，4.66x 这类倍数差异消失或大幅缩小。

## 竞争性解释

即使策略复现数字变好，也可能不是口径修复的功劳：

- 可能是参数在不同口径下恰好更优，而非口径本身正确（需做口径切换前后的 A/B 对照）。
- 可能是聚宽官方策略代码本身的执行细节（如下单时点、成交价）与回测引擎差异，而非数据口径。
- 分钟线口径切换后可能引入新的未来函数风险（前复权 vs 后复权的时点处理），需要独立审计。

## 证伪条件

出现以下情况时，本方向应 `revise` 或 `park`：

- 阶段1核心 13 表口径切换后，复现数字**没有**向聚宽官方靠拢（说明口径不是主因）。
- jqdatasdk 在全量历史拉取时出现无法绕过的速率/配额阻塞（阶段0已排除，但全量分钟线尚未实测）。
- `jq_v3_*` 路由切换导致大面积回测引擎报错且回滚成本过高（说明隔离设计有缺陷）。

## 分阶段路线图

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 阶段0 | jq_fetcher 框架（认证、断点续传、重试、配额监控、并发拉取、benchmark） | 历史阶段已完成，见 [[04_实验记录/EX-20260708T075117Z-main-6WED_阶段0基础设施验证]] |
| 阶段1 | 核心表切换到 `jq_v3_*`，并通过 `data_routing` 使用纯新表 | 已完成，当前 V2 正式口径 |
| 阶段2-4 | 扩展数据、每日更新、完整性审计和复权边界治理 | 持续推进，以 V2 导入契约和更新器手册为准 |

## 已完成实验

| ex_id | 结论 | 记录 |
| --- | --- | --- |
| EX-20260708T075117Z-main-6WED | 阶段0基础设施验证通过：工期1.7天、配额占比0.47%、延迟远低于门槛、28个单元测试全通过 | [[04_实验记录/EX-20260708T075117Z-main-6WED_阶段0基础设施验证]] |

## 关键决策

| dec_id | 决策 | 记录 |
| --- | --- | --- |
| （暂无） | 阶段0为基础设施验证，无路线分叉，未产生 DEC | 三项架构决策（接入方式/隔离策略/范围）在规格设计阶段由主控承担，未单独建卡 |

## 当前下一步

进入阶段1（核心 13 表口径切换）：
- 新建 `jq_v3_*` 表结构。
- 实现 `data_routing` 路由层，支持 jq_* ↔ jq_v3_* 切换并可回滚。
- 对 13 张主表逐一做口径切换 + 回归测试（重点是 valuation 的 PE/PB/PS、stock_info 的行业、复权因子）。
- **注意**：阶段1的分钟线全量拉取**不能**用 `fetch_panel` 的内存返回模式（会 OOM），必须用流式写 CSV（规格 §3.4）。
- 阶段1需单独写实现计划（writing-plans）。

## 给新手的方向解释

- **为什么有这个方向**：平台里 13 张表的名字和配置都写着"聚宽"，但数据其实来自另一个库（xtdata）。这导致 PE/PB/PS 是空的、行业分类是空的、复权因子和聚宽官方对不上。任何想精确复现聚宽策略的人都会被这个坑绊倒。
- **它和主策略是什么关系**：它不是某个策略的研究，而是所有策略共享的数据地基。地基偏了，上面所有策略的数字都不可信。WUFU 策略复现时出现 4.66x 倍数差异，部分就是复权因子口径偏差造成的。
- **当前最重要的证据**：阶段0已经做了一个聚宽数据拉取框架（jq_fetcher），实测证明用聚宽正式版账号纯 API 拉全部历史数据只要 1.7 天、配额够用 200 倍，技术上完全可行。
- **现在还不知道什么**：核心 13 表口径切回聚宽后，策略复现数字会不会真的向聚宽官方靠拢——这是阶段1要回答的问题。

## 子代理调用记录

本轮（阶段0成果记录、研究库资产创建）的子代理使用情况：

- **阶段0执行期间**：采用 subagent-driven-development，5 个 Task 各派实现者子代理 + 任务审查子代理 + 最终整分支审查（opus）。完整调用记录在平台仓库 progress.md（平台仓库的工程子代理记录，不属于研究库资产创建范围）。
- **本轮研究库资产创建**：调用 1 个 sonnet 子代理做格式化工作（frontmatter 填充、正文组织、台账同步），不做研究判断。研究判断（规格设计、三项架构决策、阶段划分）由主控承担。台账行：[[01_台账/子代理调用台账.csv]]。

子代理豁免说明：规格设计、三项架构决策和阶段划分属于最终研究判断，未委派子代理，由主控承担。
